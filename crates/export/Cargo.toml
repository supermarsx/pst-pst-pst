[package]
name = "pst-pst-pst-export"
version = "0.1.0"
edition = "2021"
description = "Deterministic export pipeline for mailbox and attachment artifacts."
license = "MIT"
rust-version = "1.70"

[dependencies]
pst-pst-pst-core = { path = "../core" }
